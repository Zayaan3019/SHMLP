services:
  - type: web
    name: shmlrp
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && pip install -e .
    startCommand: python -m shmlrp api --host 0.0.0.0 --port $PORT
    autoDeploy: true
